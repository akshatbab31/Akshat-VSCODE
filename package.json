{
  
  "repository": {},
  "private": true,
  "scripts": {
    "prepublish": "gulp build",
    "compile": "gulp compile",
    "build": "gulp build",
    "tslint": "gulp tslint",
    "watch": "gulp watch",
    "package": "gulp package",
    "publish": "gulp publish"
  }
}
